---
description: Sprint Phase 1 · optional — run a visual snapshot diff when visual verification is enabled.
---

As you begin this step, call `cyboflow_report_step` with `step_id` `visual-verify`.

When visual verification is enabled, run a snapshot diff. Off unless configured —
skip this step otherwise.
