---
description: Sprint Phase 1 · implement the ready task — write the diff, keep it scoped to the acceptance criteria, run local checks.
---

As you begin this step, call `cyboflow_report_step` with `step_id` `implement`.

Implement the task. Read the project's CODE-PATTERNS.md, write the diff, and run
local checks. Keep the change scoped to the task's acceptance criteria. (This step
may be retried up to 3× via loopback from `/cyboflow-task-verify`.)
