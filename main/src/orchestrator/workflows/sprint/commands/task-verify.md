---
description: Sprint Phase 1 · check the diff against the task's acceptance criteria; loop back to implement on failure.
---

As you begin this step, call `cyboflow_report_step` with `step_id` `task-verify`.

Check the diff against the task's acceptance criteria. If it fails, loop back to
`/cyboflow-implement` (up to 3× before escalating).
