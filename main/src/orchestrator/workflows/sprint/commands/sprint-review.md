---
description: Sprint Phase 2 · taste pass over the whole diff — naming, layering, CLAUDE.md drift; emit issues as findings.
---

As you begin this step, call `cyboflow_report_step` with `step_id` `sprint-review`.

Taste pass over the whole diff — naming, layering, CLAUDE.md drift. Emit any issues
you find as findings via `cyboflow_report_finding` (non-blocking); they are triaged
from the review queue, not fixed inline here.
